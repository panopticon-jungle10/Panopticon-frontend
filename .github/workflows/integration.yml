name: Integration Checks - Lint / Typecheck / Prettier / Build

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

jobs:
  checks:
    name: Lint / Typecheck / Prettier
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [21.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type-check
        run: npm run typecheck

      - name: Prettier check
        run: npm run format:check:ci

      - name: ESLint check
        run: npm run lint:ci

      - name: Build
        env:
          # 통합 테스트에서는 MSW를 비활성화 (실제 빌드 검증)
          NEXT_PUBLIC_ENABLE_MOCKING: false
        run: npm run build
